# GitHub API Configuration
# Replace with your values and rename this file to .env

# Required: Your GitHub Personal Access Token with repo scope
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token

# Required: GitHub repository URL to fork (can be in any of these formats):
# - https://github.com/owner/repo
# - git@github.com:owner/repo.git
# - owner/repo
ORIGINAL_REPO_URL=https://github.com/owner/repo

# Legacy configuration (not needed if using ORIGINAL_REPO_URL)
# ORIGINAL_OWNER=original_repo_owner
# ORIGINAL_REPO=original_repo_name

# Optional: Target branch to add the CI workflow to (defaults to repository's default branch)
# TARGET_BRANCH=main

# Optional: Type of CI system to use (defaults to github-actions if not specified)
# Available options: github-actions, gitlab-ci, circle-ci, travis-ci, jenkins, azure-pipelines, bitbucket-pipelines
CI_TYPE=github-actions

# Optional: Type of site being deployed (auto-detected if not specified)
# Available options: generic, jekyll
# SITE_TYPE=jekyll

# Optional: Type of hosting to use for branch deployments
# If both Vercel and Netlify tokens are provided, Vercel will be used by default
# Available options: vercel, netlify
HOSTING_TYPE=vercel

# Required for Vercel hosting: Your Vercel authentication token
# Create one at https://vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_auth_token

# Optional: Custom name for your Vercel project (defaults to repository name if not specified)
# VERCEL_PROJECT_NAME=your-custom-project-name

# Alternative hosting: Netlify configuration
# Required for Netlify hosting: Your Netlify personal access token
# Create one at https://app.netlify.com/user/applications#personal-access-tokens
# NETLIFY_AUTH_TOKEN=your_netlify_auth_token

# Optional: Custom name for your Netlify site (defaults to repository name if not specified)
# NETLIFY_SITE_NAME=your-custom-site-name

# Optional: Automatically create a development branch (true/false)
CREATE_DEV_BRANCH=true

# Optional: Organization name to fork to (if not specified, forks to your personal account)
# ORGANIZATION=your_organization_name 